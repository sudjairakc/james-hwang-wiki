---
import WikiLayout from '../../layouts/WikiLayout.astro';
import { langStaticPaths, type Lang } from '../../i18n';
import { getCollaborations, collabChart } from '../../data/pages/collaborations';

export function getStaticPaths() {
  return langStaticPaths();
}

const lang = Astro.params.lang as Lang;
const base = import.meta.env.BASE_URL;
const d = getCollaborations(lang);

const relatedLabels = {
  discography: { en: 'Discography', th: 'ผลงานเพลง' },
  canon: { en: 'Artist Canon', th: 'Artist Canon' },
  awards: { en: 'Awards', th: 'รางวัล' },
  cultural: { en: 'Cultural Impact', th: 'อิทธิพลเชิงวัฒนธรรม' },
};
const related = [
  { label: relatedLabels.discography[lang], href: `${base}${lang}/discography` },
  { label: relatedLabels.canon[lang], href: `${base}${lang}/canon` },
  { label: relatedLabels.awards[lang], href: `${base}${lang}/awards` },
  { label: relatedLabels.cultural[lang], href: `${base}${lang}/cultural-impact` },
];
---

<WikiLayout title={d.ui.title} section="Music" lang={lang} relatedPages={related}>

  <!-- Header -->
  <div class="mb-8">
    <div class="text-xs font-semibold uppercase tracking-widest mb-1" style="color:var(--gold)">{d.ui.label}</div>
    <h1 class="wiki-title mb-3">{d.ui.title}</h1>
    <p class="text-sm leading-relaxed" style="color:var(--text-muted)">
      {d.ui.intro}
    </p>
  </div>

  <!-- Summary stats -->
  <div class="grid grid-cols-2 md:grid-cols-4 gap-3 mb-10">
    {d.stats.map(s => (
      <div class="stat-card">
        <div class="stat-val">{s.val}</div>
        <div class="stat-label">{s.label}</div>
      </div>
    ))}
  </div>

  <!-- Streams chart -->
  <section class="mb-10">
    <div class="text-xs font-semibold uppercase tracking-widest mb-1" style="color:var(--gold)">{d.ui.chartHeading}</div>
    <p class="text-xs mb-3" style="color:var(--text-faint)">{d.ui.chartSub}</p>
    <div class="rounded-xl border overflow-hidden p-4" style="border-color:var(--border);background-color:var(--bg-surface)">
      <canvas id="collabChart" height="180" style="width:100%;display:block"></canvas>
      <div class="flex gap-4 mt-3">
        <div class="flex items-center gap-2"><span class="inline-block w-3 h-3 rounded-sm" style="background-color:#1db954cc"></span><span class="text-xs" style="color:var(--text-muted)">{d.ui.legendSpotify}</span></div>
        <div class="flex items-center gap-2"><span class="inline-block w-3 h-3 rounded-sm" style="background-color:#ff000088"></span><span class="text-xs" style="color:var(--text-muted)">{d.ui.legendYoutube}</span></div>
      </div>
    </div>
  </section>

  <!-- Featured Tracks section -->
  <div class="mb-12">
    <div class="flex items-center gap-3 mb-6">
      <div class="text-xs font-semibold uppercase tracking-widest" style="color:var(--gold)">{d.ui.featuredHeading}</div>
      <div class="flex-1 h-px" style="background-color:var(--border)"></div>
      <div class="text-xs" style="color:var(--text-faint)">{d.ui.featuredSub}</div>
    </div>

    <div class="space-y-4">
      {d.featuredTracks.map(t => (
        <div class="rounded-xl border overflow-hidden" style="border-color:var(--border)">
          <!-- Title bar -->
          <div class="flex items-center justify-between px-5 py-3.5 border-b" style={`background-color:${t.eraColor}10;border-color:${t.eraColor}30`}>
            <div>
              <div class="font-semibold text-sm" style="color:var(--text)">{t.title}</div>
              <div class="text-xs mt-0.5" style="color:var(--text-muted)">{t.genre}</div>
            </div>
            <div class="flex items-center gap-2 shrink-0">
              <span class="text-xs px-2 py-0.5 rounded font-semibold" style={`background-color:${t.eraColor}18;color:${t.eraColor}`}>{t.year}</span>
            </div>
          </div>

          <!-- Details grid -->
          <div class="grid grid-cols-1 md:grid-cols-2 gap-0" style="background-color:var(--bg-surface)">
            <!-- Left: metadata -->
            <div class="px-5 py-4 space-y-2.5 border-b md:border-b-0 md:border-r" style="border-color:var(--border)">
              {[
                { label: d.ui.labelFeaturedArtist, val: t.artist },
                { label: d.ui.labelRole, val: t.artistRole },
                { label: d.ui.labelAlbum, val: t.album },
              ].map(r => (
                <div class="flex gap-3">
                  <span class="text-xs font-semibold w-28 shrink-0 mt-0.5" style="color:var(--text-muted)">{r.label}</span>
                  <span class="text-sm">{r.val}</span>
                </div>
              ))}
              <div class="flex gap-3 pt-1">
                <span class="text-xs font-semibold w-28 shrink-0 mt-0.5" style="color:var(--text-muted)">{d.ui.labelStreams}</span>
                <div class="flex gap-3 flex-wrap">
                  <span class="text-sm font-semibold" style={`color:${t.eraColor}`}>Spotify {t.spotify}</span>
                  {t.youtube && <span class="text-sm" style="color:var(--text-muted)">YT {t.youtube}</span>}
                </div>
              </div>
              <div class="flex gap-3">
                <span class="text-xs font-semibold w-28 shrink-0 mt-0.5" style="color:var(--text-muted)">{d.ui.labelAlbum}</span>
                <a href={`${base}${lang}/discography/${t.albumSlug}`} class="text-sm hover:underline" style={`color:${t.eraColor}`}>
                  → {t.album}
                </a>
              </div>
            </div>
            <!-- Right: context note -->
            <div class="px-5 py-4 flex items-start">
              <p class="text-sm leading-relaxed" style="color:var(--text-muted)">{t.note}</p>
            </div>
          </div>
        </div>
      ))}
    </div>
  </div>

  <!-- SM Station collaborations -->
  <div class="mb-12">
    <div class="flex items-center gap-3 mb-6">
      <div class="text-xs font-semibold uppercase tracking-widest" style="color:var(--gold)">{d.ui.stationHeading}</div>
      <div class="flex-1 h-px" style="background-color:var(--border)"></div>
      <div class="text-xs" style="color:var(--text-faint)">{d.ui.stationSub}</div>
    </div>

    <div class="rounded-xl overflow-hidden border" style="border-color:var(--border)">
      {d.stationSingles.map((t, i) => (
        <div class={`flex flex-col md:flex-row gap-0 ${i !== d.stationSingles.length - 1 ? 'border-b' : ''}`} style="border-color:var(--border)">
          <div class="w-1.5 shrink-0" style={`background-color:${t.eraColor}`}></div>
          <div class="flex-1 px-5 py-4" style="background-color:var(--bg-surface)">
            <div class="flex flex-wrap items-start justify-between gap-2 mb-2">
              <div>
                <div class="font-semibold text-sm" style="color:var(--text)">{t.title}</div>
                <div class="text-xs mt-0.5" style="color:var(--text-muted)">{t.platform} · {t.year} · with {t.partner} ({t.partnerGroup})</div>
              </div>
            </div>
            <p class="text-sm leading-relaxed" style="color:var(--text-muted)">{t.note}</p>
          </div>
        </div>
      ))}
    </div>

    <div class="mt-3 text-xs rounded-lg px-4 py-3 border-l-2" style="border-color:var(--gold);background-color:color-mix(in srgb, var(--gold) 6%, transparent);color:var(--text-muted)">
      {d.ui.stationCallout}
    </div>
  </div>

  <!-- Collaboration philosophy -->
  <div class="rounded-xl border p-6 mb-8" style="border-color:var(--border);background-color:var(--bg-surface)">
    <div class="text-xs font-semibold uppercase tracking-widest mb-4" style="color:var(--gold)">{d.ui.philosophyHeading}</div>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6 text-sm" style="color:var(--text-muted)">
      <div>
        <div class="font-semibold mb-1.5" style="color:var(--text)">{d.ui.philosophySelectionTitle}</div>
        <p class="leading-relaxed">{d.ui.philosophySelectionBody}</p>
      </div>
      <div>
        <div class="font-semibold mb-1.5" style="color:var(--text)">{d.ui.philosophyTermsTitle}</div>
        <p class="leading-relaxed">{d.ui.philosophyTermsBody}</p>
      </div>
      <div>
        <div class="font-semibold mb-1.5" style="color:var(--text)">{d.ui.philosophyPacingTitle}</div>
        <p class="leading-relaxed">{d.ui.philosophyPacingBody}</p>
      </div>
    </div>
  </div>

</WikiLayout>

<script is:inline define:vars={{ collabChart }}>
(function () {
  const canvas = document.getElementById('collabChart');
  if (!canvas) return;
  const W = canvas.parentElement.clientWidth - 32;
  const H = 180;
  const dpr = window.devicePixelRatio || 1;
  canvas.width = W * dpr; canvas.height = H * dpr;
  canvas.style.width = W + 'px'; canvas.style.height = H + 'px';
  const ctx = canvas.getContext('2d');
  ctx.scale(dpr, dpr);

  const cs = getComputedStyle(document.documentElement);
  const textFaint = cs.getPropertyValue('--text-faint').trim() || 'rgba(255,255,255,0.25)';
  const borderCol = cs.getPropertyValue('--border').trim()     || 'rgba(255,255,255,0.1)';

  const PAD = { l: 16, r: 56, t: 16, b: 36 };
  const cw = W - PAD.l - PAD.r;
  const ch = H - PAD.t - PAD.b;
  const N = collabChart.length;
  const maxVal = Math.max(...collabChart.map(d => d.spotify + (d.youtube || 0))) * 1.1;

  const slotH = ch / N;
  const barH  = Math.min(slotH * 0.3, 14);
  const gap   = barH * 0.5;

  // grid verticals
  [0.25, 0.5, 0.75, 1].forEach(f => {
    const x = PAD.l + f * cw;
    ctx.strokeStyle = borderCol; ctx.lineWidth = 1; ctx.setLineDash([2, 4]);
    ctx.beginPath(); ctx.moveTo(x, PAD.t); ctx.lineTo(x, PAD.t + ch); ctx.stroke();
    ctx.setLineDash([]);
    ctx.font = '9px system-ui,sans-serif'; ctx.fillStyle = textFaint;
    ctx.textAlign = 'center'; ctx.textBaseline = 'top';
    ctx.fillText(Math.round(maxVal * f / 1.1) + 'M', x, PAD.t + ch + 4);
  });

  collabChart.forEach((d, i) => {
    const midY = PAD.t + i * slotH + slotH / 2;

    // Spotify bar
    const sw = (d.spotify / maxVal) * cw;
    ctx.fillStyle = '#1db954cc';
    ctx.beginPath(); ctx.roundRect(PAD.l, midY - barH - gap / 2, sw, barH, [0, 3, 3, 0]); ctx.fill();
    ctx.font = '700 9px system-ui,sans-serif';
    ctx.fillStyle = '#1db954'; ctx.textAlign = 'left'; ctx.textBaseline = 'middle';
    ctx.fillText(d.spotify + 'M', PAD.l + sw + 4, midY - barH / 2 - gap / 2);

    // YouTube bar
    if (d.youtube) {
      const yw = (d.youtube / maxVal) * cw;
      ctx.fillStyle = '#ff000077';
      ctx.beginPath(); ctx.roundRect(PAD.l, midY + gap / 2, yw, barH, [0, 3, 3, 0]); ctx.fill();
      ctx.font = '700 9px system-ui,sans-serif';
      ctx.fillStyle = '#ff6666'; ctx.textAlign = 'left'; ctx.textBaseline = 'middle';
      ctx.fillText(d.youtube + 'M', PAD.l + yw + 4, midY + barH / 2 + gap / 2);
    }

    // track label + year
    ctx.font = '700 10px system-ui,sans-serif';
    ctx.fillStyle = d.color; ctx.textAlign = 'right'; ctx.textBaseline = 'bottom';
    ctx.fillText(d.title, PAD.l - 4, midY - gap / 2);
    ctx.font = '500 9px system-ui,sans-serif';
    ctx.fillStyle = textFaint; ctx.textBaseline = 'top';
    ctx.fillText(d.artist + ' · ' + d.year, PAD.l - 4, midY + gap / 2);
  });
})();
</script>
